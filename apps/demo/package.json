{
  "name": "@rich-editor/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-standalone.mjs && vite",
    "build": "node scripts/sync-standalone.mjs && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@rich-editor/vue": "0.1.0",
    "vue": "^3.5.42"
  }
}
